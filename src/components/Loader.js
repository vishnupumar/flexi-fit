import React from "react";

export const Loader = () => {
  return (
    <div
      style={{
        height: "30vh",
        justifyContent: "center",
        display: "flex",
        alignItems: "center",
      }}
    >
      Loading...
    </div>
  );
};
